const width = {
  fluid: 'fluid',
  small: 'small',
  medium: 'medium',
  large: 'large'
};

const sizeCore = {
  none: 'none',
  small: 'small',
  medium: 'medium',
  large: 'large',
  xlarge: 'xlarge'
};

const xPadding = { ...sizeCore };
const yPadding = { ...sizeCore };
const topPadding = { ...sizeCore };
const bottomPadding = { ...sizeCore };

const xMargin = { ...sizeCore };
const yMargin = { ...sizeCore };
const topMargin = { ...sizeCore };
const bottomMargin = { ...sizeCore };

export { width, xPadding, yPadding, topPadding, bottomPadding, yMargin, xMargin, topMargin, bottomMargin, sizeCore };
