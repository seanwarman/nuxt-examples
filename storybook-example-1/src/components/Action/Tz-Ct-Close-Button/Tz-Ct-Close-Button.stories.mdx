import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import { withDesign } from 'storybook-addon-designs';

import TzCtCloseButton from './Tz-Ct-Close-Button.vue';

<Meta title="Components/Action/CT Close Button" component={TzCtCloseButton} decorators={[withDesign]} />

export const Template = (args) => ({
  components: { TzCtCloseButton },
  template: `<tz-ct-close-button @click="clicked" />`,
  methods: {
    clicked: action('clicked')
  }
});

## Example

<Canvas withSource="open">
  <Story name="Default" args={{}} parameters={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
