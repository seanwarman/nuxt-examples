import { Meta, Story } from '@storybook/addon-docs';

import TzModalDialog from '@/components/Action/Tz-ModalDialog/Tz-ModalDialog.vue';

<Meta title="components/Action/ModalDialog" component={TzModalDialog} argTypes={{}} />

export const Template = (args) => ({
  components: { TzModalDialog },
  setup() {
    return { args };
  },
  template: `
  <tz-modal-dialog :show="true">
    <div class="w-[600px]">
      <h2 class="mb-8 text-center text-base font-black uppercase leading-5 tracking-wider">Modal Dialog</h2>
      <p>The modal dialog is a simple modal container with an optional close button.</p>
      <p class="mb-0">It has accessibility features such as:</p>
      <ul class="pl-2 mb-4">
        <li>Trapping focus within the modal</li>
        <li>Returning focus to the element that opened it</li>
        <li>Closing with the Esc key</li>
        <li>Aria describing it</li>
      </ul>
      <p class="mb-0">It is intended to be used within a Higher Order Component where the HOC adds content to it.</p>
    </div>
  </tz-modal-dialog>
  `
});

# Modal

<Story name="Default Final Modal" args={{}} parameters={{}}>
  {Template.bind({})}
</Story>
