import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import TzHeader from './Tz-Header.vue';
import { withDesign } from 'storybook-addon-designs';
import { action } from '@storybook/addon-actions';

<Meta title="Header/Site Header" decorators={[withDesign]} argTypes={{}} component={TzHeader} />

export const Template = (args) => ({
  components: { TzHeader },
  setup() {
    return { args };
  },
  template: `<tz-header 
    v-bind='args' 
    @logoClicked="logoClicked" 
    @menuClicked="menuClicked" 
    @searchClicked="searchClicked"
    @wishlistClicked="wishlistClicked" 
    @basketClicked="basketClicked" 
    @myAccountClicked="myAccountClicked"
  ></tz-header>`,
  methods: {
    logoClicked: action('logoClicked'),
    menuClicked: action('menuClicked'),
    searchClicked: action('searchClicked'),
    wishlistClicked: action('wishlistClicked'),
    basketClicked: action('basketClicked'),
    myAccountClicked: action('myAccountClicked')
  }
});

# Site Header

<Canvas>
  <Story name="Demo" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
