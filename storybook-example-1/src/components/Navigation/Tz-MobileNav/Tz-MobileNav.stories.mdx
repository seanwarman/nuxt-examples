import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import TzMobileNav from './Tz-MobileNav.vue';
import { navigation } from './Tz-Navigation.options';

<Meta
  title="Components/Navigational/Mobile Navigation"
  component={TzMobileNav}
  argTypes={{
    default: {
      control: { type: 'number' }
    }
  }}
/>

export const Template = (args) => ({
  components: { TzMobileNav },
  setup() {
    return { args };
  },
  template: `<tz-mobile-nav v-bind="args"></tz-mobile-nav>`
});

# Mobile Navigation

Mobile navigation layout.

<Canvas>
  <Story
    name="Default"
    args={{
      menu: navigation
    }}
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/proto/f9CMMSAWFsKLRUa9xPbpGr/design-system?page-id=60%3A129&node-id=69%3A189&viewport=340%2C48%2C0.47&scaling=min-zoom'
      },
      viewport: {
        //👇default viewport 'Large Mobile'
        defaultViewport: 'mobile2'
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props & slots

<ArgsTable story="Default" />
