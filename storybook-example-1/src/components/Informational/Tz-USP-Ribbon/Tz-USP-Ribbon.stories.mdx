import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import TzUSPRibbon from './Tz-USP-Ribbon.vue';

<Meta title="components/Informational/USP Ribbon" component={TzUSPRibbon} />

export const Template = (args) => ({
  components: { TzUSPRibbon },
  setup() {
    return { args };
  },
  template: '<tz-USP-ribbon v-bind="args" />'
});

# Banner

<Canvas withSource="open">
  <Story
    name="Default"
    args={{
      offerTitles: ['Free Delivery', '0% APR Finance', 'Price Match']
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

<Canvas withSource="open">
  <Story
    name="Mobile"
    args={{
      offerTitles: ['Free Delivery', '0% APR Finance', 'Price Match']
    }}
    parameters={{
      viewport: {
        defaultViewport: 'mobile1'
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
