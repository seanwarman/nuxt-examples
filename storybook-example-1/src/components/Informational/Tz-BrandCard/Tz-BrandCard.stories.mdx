import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import TzBrandCard from './Tz-BrandCard.vue';
import { brandCard } from './Tz-BrandCard.data';

<Meta title="components/Informational/Brand Card" component={TzBrandCard} />

export const Template = (args) => ({
  components: { TzBrandCard },
  setup() {
    return { args };
  },
  template: '<tz-brand-card v-bind="args"/>'
});

# Brand Card

<Canvas withSource="open">
  <Story
    name="Default"
    args={{
      ...brandCard
    }}
    parameters={{
      viewport: {
        defaultViewport: 'mobile1'
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
