import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import TzLozenge from './Tz-Lozenge.vue';

<Meta title="components/Informational/Lozenge" component={TzLozenge} />

export const Template = (args) => ({
  components: { TzLozenge },
  setup() {
    return { args };
  },
  template: '<tz-lozenge v-bind="args">CYCLE TO WORK</tz-lozenge>'
});

# Lozenge

<Canvas withSource="open">
  <Story
    name="Default"
    args={{
      label: 'CYCLE TO WORK',
      color: 'orange',
      radius: 3
    }}
    parameters={{
      viewport: {
        defaultViewport: 'mobile1'
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
